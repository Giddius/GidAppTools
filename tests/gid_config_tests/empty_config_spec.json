{
    "a_section": {
        "a_key": {
            "converter": "string",
            "description": "This is a description.",
            "default": "this is a default"
        }
    },
    "a_created_section": { "a_created_key": { "converter": "boolean" } }
}
